{
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:ci": "jest --ci --reporters=jest-junit --reporters=default --coverage",
    "test:cov": "jest --coverage",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:e2e": "jest --config ./test/jest-e2e.json",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "csv-parser": "^3.0.0",
    "pg": "^8.12.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/mssql": "^9.1.5",
    "@types/pg": "^8.11.6",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  },
  "directories": {
    "test": "tests"
  },
  "name": "data2pgdb",
  "description": "`data2pgdb` is a powerful utility designed to simplify the process of uploading data from Excel files directly into your PostgreSQL database. With minimal setup, you can map Excel columns to database fields and insert the data efficiently. This tool is ideal for developers and data professionals who need a straightforward solution for data migration or regular data imports.",
  "version": "1.0.5",
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/youssefelya/data2pgdb.git"
  },
  "author": "Youssef EL Yamani",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/youssefelya/data2pgdb/issues"
  },
  "homepage": "https://github.com/youssefelya/data2pgdb#readme"
}
